var srcDir = 'src';
var buildDir = 'dist';

module.exports = {
  srcDir: srcDir,
  srcStyles: srcDir + '/styles/**/*.sass',
  srcScripts: srcDir + '/**/*.js',
  buildDir: buildDir,
  buildStylesDir: buildDir + '/styles'
};
